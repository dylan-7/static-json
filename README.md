# static-json
📄 utils

# Installation
```
yarn add static-json
```

# Usage

Return json list.

```
import { payType } from 'static-json';
payType.map(v => <span>v</span>);
```

+ payType
+ yesNo
+ terminal
